---
import StateEvent from "../../../components/StateEvent.astro";
import { readPublishedState, eventPath } from "../../../lib/state.mjs";
import { event as prose } from "../../../data/fomc20260617.mjs";
const view = readPublishedState().events.get("event-fed-rate-june-2026");
if (!view) throw new Error("Missing story view: event-fed-rate-june-2026");
if (view.status === "merged") return Astro.redirect(eventPath(view.survivor), 301);
---
<StateEvent view={view} prose={prose.dek} />
