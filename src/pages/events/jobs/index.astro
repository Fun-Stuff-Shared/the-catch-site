---
import { readState, chainPath } from "../../../lib/state.mjs";
const view = readState().events.get("event-jobs-july-2026");
if (!view?.root_id) throw new Error("Missing story chain: event-jobs-july-2026");
return Astro.redirect(chainPath(view.root_id), 301);
---
