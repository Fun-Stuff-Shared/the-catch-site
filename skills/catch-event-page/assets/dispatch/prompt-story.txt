Use the catch-story skill at skills/catch-story in this checkout, at skill commit {SKILL_COMMIT}. Read its SKILL.md in full, then its references/story.md, shape-rules.md, sections.md, components.md, writing.md and procedures.md in full, before acting; do not edit any file under skills/. This run is TURN THREE of the three-turn author: write the story on a record one session built and a reader model another session wrote.

Working checkout for this run: {WORKTREE} (a git worktree of the-catch-site on branch {BRANCH}). Every path the skill writes as /Volumes/4/GitHub/the-catch-site means {WORKTREE} for this run: read, write, build and commit only there. Do not touch /Volumes/4/GitHub/the-catch-site itself.

The record is the commit whose message begins "record:" and the reader model is the commit whose message begins "structure:" (checks/reader-models/{SUBJECT}--{SLUG}.md: Entering, Exiting with the seven answers and record ids, Grades, Sections, an Outline when present). You write in its order and do not rewrite its answers; you add to it only to grade a passage you admit on the way. The completeness audit already ran on the record commit and its findings are graded in that file; it does not run again. Turn one left: the working note under checks/working-notes/, the pins under data/sources/{SUBJECT}/, the data module under src/data/, the page under src/pages/events/{SUBJECT}/{SLUG}.astro with its figures, chronology and detail blocks and no narrative, the manifest. Read all of it first; then the built page (npm run build, then dist/events/{SUBJECT}/{SLUG}/index.html) in all three modes.

First admit every gap line the reader model graded A or B (fetch, pin, census line, passage table, manifest row; recover a refused fetch as the record procedures do). Then do the skill's steps 5, 6 and 7 as written: the story written one passage at a time with the pin open, in the reader model's sections and order (the story view carries A and B material only; concept before qualification; no section that checks a claim nobody made; the page never names its reader, never calls itself "this page", never speaks in the first person outside a quotation); skills/catch-story/scripts/finish.sh {SUBJECT}/{SLUG} story --no-commit green (the build with the gate and the three lints at zero); the interrogation run and dispositioned; the entailment check on your tree returning ENTAILED with its verdict file kept for the commit; the subject page row and the homepage feature checked against the story as written; then skills/catch-story/scripts/finish.sh {SUBJECT}/{SLUG} story, the one command that commits the turn. Never commit by hand, never run any state command, never commit anything under data/state. If a passage the story needs is missing from the record, admit it through capture news, write its census line in the working note and grade it in the reader model.

Do NOT push. Do NOT edit any existing story page. Do not kill, restart, or signal any process you did not start. When done, print a final report in plain words: the section list, what you could not do and why, the local path of the built page, and the commit hash. Everything you claim must be checkable from files in the repo.
