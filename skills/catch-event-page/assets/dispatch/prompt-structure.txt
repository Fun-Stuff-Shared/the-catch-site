Use the catch-structure skill at skills/catch-structure in this checkout, at skill commit {SKILL_COMMIT}. Read its SKILL.md in full, then its references/story.md and references/sections.md in full, before acting; do not edit any file under skills/. This run is TURN TWO of the three-turn author: the structure. You write one file, the reader model, and commit it. You write no story sentence and change no page.

Working checkout for this run: {WORKTREE} (a git worktree of the-catch-site on branch {BRANCH}). Every path the skill writes as /Volumes/4/GitHub/the-catch-site means {WORKTREE} for this run: read, write and commit only there. Do not touch /Volumes/4/GitHub/the-catch-site itself.

The record is the commit on this branch whose message begins "record:" (git log --grep "^record:" -1). Turn one left: the working note at checks/working-notes/{SUBJECT}--{SLUG}.md, the pins and text siblings under data/sources/{SUBJECT}/, the data module under src/data/, the page under src/pages/events/{SUBJECT}/ with its figures, chronology and detail blocks and no narrative, the manifest. The completeness audit already ran on that commit: {AUDIT_FILE}. Read all of it, and the built page at dist/events/{SUBJECT}/{SLUG}/index.html (npm run build if dist is missing). You did not build this record and you do not know how hard any of it was to get; that is the point.

Then do the skill's one step as written, in the shape of skills/catch-structure/assets/reader-model.md: checks/reader-models/{SUBJECT}--{SLUG}.md with "## Entering", "## Exiting" (the seven answers, each with its record id), "## Grades" (every passage-table line, gap line and record-audit finding, A to D, and for A and B the answer it serves), "## Sections" (the core plus only the optional sections the question tree asks for, each with its question, in the reader's order), and "## Outline" if the story needs one. An answer with no record is a gap line in the working note, graded, for the story turn to admit; so is every record-audit finding that cites a document outside the manifest (write it into "Not admitted this run" with the audit's link, graded by what it does for the seven answers, never C because it is unpinned); do not admit records yourself. Then the one command that ends the turn: skills/catch-structure/scripts/finish.sh {SUBJECT}/{SLUG} structure. Never commit by hand, never run any state command, never commit anything under data/state.

Do NOT push. Do not kill, restart, or signal any process you did not start. When done, print a final report in plain words: the Entering paragraph, the seven answers, the section list, and the commit hash. Everything you claim must be checkable from files in the repo.
